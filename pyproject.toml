[project]
name = "finance-stock-model"
version = "0.1.0"
description = "Train and evaluate a next-day stock close model"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2,<3",
    "scikit-learn>=1.5,<2",
    "statsmodels>=0.14,<1",
    "torch>=2.2,<3",
]

[tool.uv]
package = false
